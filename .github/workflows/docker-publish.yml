name: Publish Docker image

# Ordinary pushes to main publish to :development only — safe to land continuously
# without touching what anyone's actually running. :latest (production) only updates
# when a version tag (v1.2.3) is pushed, so a release is a deliberate, separate step.
on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch: {}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Compute tags
        id: tags
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
            TAGS="therealbatbro/remotarr:latest,therealbatbro/remotarr:${VERSION}"
          else
            TAGS="therealbatbro/remotarr:development"
          fi
          TAGS="${TAGS},therealbatbro/remotarr:${GITHUB_SHA}"
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"

      - name: Build (local, for scanning before it ever reaches Docker Hub)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          provenance: false
          sbom: false
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan image for vulnerabilities
        uses: aquasecurity/trivy-action@0.28.0
        with:
          # Any of the tags computed above resolves to the same just-built image.
          image-ref: therealbatbro/remotarr:${{ github.sha }}
          format: table
          # A tagged release (v1.2.3) fails the build on a CRITICAL/HIGH finding with a known
          # fix — a base image or dependency CVE severe enough to block :latest shouldn't ship
          # silently. Ordinary main pushes only report (:development is meant to land
          # continuously, per the note above) so this doesn't block every commit.
          exit-code: ${{ startsWith(github.ref, 'refs/tags/v') && '1' || '0' }}
          severity: CRITICAL,HIGH
          ignore-unfixed: true

      - name: Push
        # No tag specified pushes every locally-tagged reference for this repository (all four
        # computed above) in one call.
        run: docker push therealbatbro/remotarr

      - name: Sync Docker Hub overview
        uses: peter-evans/dockerhub-description@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: therealbatbro/remotarr
          readme-filepath: ./DOCKERHUB.md
          short-description: Self-hosted web dashboard for Sonarr, Radarr, Plex, and more
